spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: root
    driver: com.mysql.cj.jdbc.Driver
server:
  servlet:
    context-path: /myapp

